const e=new WebSocket(`ws://${location.host}`);e.addEventListener("open",()=>{console.log("🟢 WebSocket connected"),e.send(JSON.stringify({type:"join",message:"Hello from client!"}))});e.addEventListener("message",o=>{const n=JSON.parse(o.data);console.log("📩 Received from server:",n)});e.addEventListener("close",()=>{console.log("🔴 WebSocket disconnected")});e.addEventListener("error",o=>{console.error("⚠️ WebSocket error:",o)});
